# ============ Frontend (Vite) ============
# Backend API base URL (required). Must include /api prefix.
# All frontend API calls use this via src/config/api.js — no hardcoded URLs.
#
# Development (copy to .env, backend e.g. on port 5000):
# VITE_API_BASE_URL=http://localhost:5000/api
#
# Production: set in .env.production (no localhost). Used by: npm run build
# VITE_API_BASE_URL=https://your-api-domain.com/api
#
VITE_API_BASE_URL=http://localhost:5000/api
